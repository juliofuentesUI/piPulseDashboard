node_modules/
dist/
build/

.env
.env.local
.env.*.local

*.log
npm-debug.log*
.npm

.vite/
.svelte-kit/
*.tsbuildinfo

.playwright-mcp/

.DS_Store
Thumbs.db

.vscode/*
!.vscode/extensions.json
.idea/

# Per-machine permission grants. The skills beside it are shared on purpose.
.claude/settings.local.json

# Local trend history (SQLite)
data/

# API keys carried to the Pi by hand — USB stick or one scp, never by git.
# Same rule as .env: this file exists so keys reach a keyboard-less Pi without
# ever being committed. They were committed once, on 2026-08-05, and rotated
# afterwards; this is what replaced that.
scripts/pi-keys.env
